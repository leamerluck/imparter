<br/>
<div class="booyah-box col-xs-10 col-xs-offset-1">
	<h1 class="text-center">List of Your Courses Offered</h1>
	<% @courses.each do |course| %>
		<h2><%= link_to course.title, instructor_course_path(course) %></h2> 
	<% end %>

	
</div>